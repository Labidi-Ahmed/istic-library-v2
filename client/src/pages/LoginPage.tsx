import Login from '../components/auth/Login';

const LoginPage = () => {
  return (
    <section className="flex justify-center bg-muted/10 items-center h-screen p-5">
      <Login />
    </section>
  );
};

export default LoginPage;
